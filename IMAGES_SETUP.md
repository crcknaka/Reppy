# Настройка изображений для упражнений

## ✅ Что уже сделано:

1. **База данных обновлена**:
   - Добавлено поле `image_url` в таблицу `exercises`
   - Миграция обновлена с путями к изображениям

2. **TypeScript типы обновлены**:
   - Добавлена поддержка `image_url` в типах Supabase

3. **Интерфейс обновлён**:
   - Страница "Упражнения" теперь показывает изображения
   - Если изображения нет, показывается иконка (как раньше)

4. **Создана папка** `public/exercises/` для хранения изображений

## 📋 Что нужно сделать:

### Шаг 1: Сохраните изображения в папку `public/exercises/`

Из загруженных изображений сохраните их с такими именами:

| Файл | Упражнение | Описание |
|------|-----------|----------|
| `shoulder-press.jpg` | Гантели Плечи | 1-е изображение - жим гантелей сидя |
| `bicep-curl.jpg` | Гантели Бицепс | 2-е изображение - сгибания на бицепс |
| `pushups.jpg` | Отжимания | 3-е изображение - отжимания |
| `pullups.jpg` | Подтягивания | 4-е изображение - подтягивания на перекладине |
| `abs.jpg` | Пресс | 5-е изображение - скручивания на пресс |
| `squats.jpg` | Приседания | 6-е изображение - приседания |
| `cable-row.jpg` | Тяга на себя | 8-е изображение - тяга блока к себе |

**Для "Штанга лёжа"** нужно добавить изображение отдельно:
- `bench-press.jpg` - жим штанги лёжа

### Шаг 2: Примените изменения в базе данных

Выберите один из способов:

#### Способ 1: Сброс базы данных (рекомендуется для новой БД)
```bash
cd supabase
supabase db reset
```

#### Способ 2: Обновление существующей БД (если уже есть данные)

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- Добавить колонку image_url (если её ещё нет)
ALTER TABLE public.exercises ADD COLUMN IF NOT EXISTS image_url TEXT;

-- Обновить существующие упражнения
UPDATE public.exercises SET image_url = '/exercises/pushups.jpg' WHERE name = 'Отжимания';
UPDATE public.exercises SET image_url = '/exercises/squats.jpg' WHERE name = 'Приседания';
UPDATE public.exercises SET image_url = '/exercises/abs.jpg' WHERE name = 'Пресс';
UPDATE public.exercises SET image_url = '/exercises/pullups.jpg' WHERE name = 'Подтягивания';
UPDATE public.exercises SET image_url = '/exercises/bench-press.jpg' WHERE name = 'Штанга лёжа';
UPDATE public.exercises SET image_url = '/exercises/shoulder-press.jpg' WHERE name = 'Гантели Плечи';
UPDATE public.exercises SET image_url = '/exercises/bicep-curl.jpg' WHERE name = 'Гантели Бицепс';
UPDATE public.exercises SET image_url = '/exercises/cable-row.jpg' WHERE name = 'Тяга на себя';
```

### Шаг 3: Перезапустите приложение

```bash
npm run dev
```

## 🎨 Результат:

После выполнения всех шагов:
- На странице "Упражнения" будут отображаться фотографии
- Изображения будут квадратными 64x64 пикселя с округлёнными углами
- Если изображения нет - будет показана иконка (как раньше)
- Изображения адаптивны и красиво выглядят на всех устройствах

## 📝 Примечания:

- Изображения хранятся локально в папке `public/exercises/`
- Рекомендуемый размер изображений: 800x600px или больше
- Формат: JPG (можно использовать PNG, но JPG легче)
- Приложение автоматически масштабирует изображения под нужный размер

